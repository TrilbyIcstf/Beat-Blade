using UnityEngine;

public enum AttackColor
{
    RED,
    BLUE,
    BLACK
}

public enum ArrowMovementType { 
    STATIC,
    TRACKING,
    HORIZONTAL,
    VERTICAL
}

public enum ArrowSpawnMethods
{
    FADE,
    MOVINGFADE
}